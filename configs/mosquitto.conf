# Dev/test-only broker config for the docker-compose mosquitto service.
# Anonymous + unencrypted, matching the closed OT network assumption for
# local development. A production deployment must set username/password
# and TLS in MQTTConfig and configure the broker to require them.
listener 1883
allow_anonymous true
persistence false
