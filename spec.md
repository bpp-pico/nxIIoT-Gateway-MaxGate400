# nxIIoT Gateway — gateway component (split for maisvch MaxGate400)

Modbus RTU/TCP acquisition → local SQLite store & forward → MQTT delivery to a downstream server. This repo was split from the original nxIIoT Gateway monorepo to deploy the gateway component onto a maisvch MaxGate400 device. For the deep architecture rationale and bug history, see [HANDOFF.md](HANDOFF.md).

## Architecture

- **Acquisition** (`internal/acquisition`) — Modbus config is split into `connection` (the physical link — protocol/interface-or-ip+port/baud/parity/timeout/retry/`next_device_delay_ms`, `internal/connection`) and `device` (a slave ID + its data points, referencing one `connection_id`, `internal/device`) — several devices can share one physical RTU bus (real multi-drop). Polls one goroutine **per enabled connection**, not per device: `Poller.runConnection` owns a single `modbus.Client` for the connection's lifetime and calls `SetUnitID` before each sibling device's own reads, serialized by construction. Devices are scanned continuously, round-robin, paced only by `next_device_delay_ms` between devices; each device's datapoints are batched into as few Modbus block reads as possible (`internal/acquisition/blockplan.go`) instead of one request per datapoint.
- **Queue** (`internal/queue`) — persists every reading to SQLite (WAL) as `data_queue` with an atomic per-gateway `sequence_id`.
- **Forwarder** (`internal/forwarder`) — ticks on `forwarder.poll_interval_ms` and dispatches batches through an `Adapter` interface — `MQTTAdapter` in production (QoS 1 + application-level ack over a dedicated ack topic), `HTTPAdapter` for dev/test. Acquisition and forwarding never block each other (Rule 1) — a dead broker just grows the local queue.
- **Deployment target**: native/systemd (not Docker — needed for `/dev/rtc0` and real `nmcli`). `deploy/nxiiot-gateway.service` (`Restart=always`) is the reference unit.

## Done

- MVP Phases 0-8 (Modbus engine, device/datapoint CRUD, Store & Forward with crash recovery, MQTT adapter, Time Service, chaos-tested reliability) — see HANDOFF.md for detail.
- Post-MVP: Settings save (Gateway/MQTT/Time → `config.yaml` + auto-restart) and host network IP config (`internal/netconfig`, `nmcli`-backed, confirm-or-auto-revert safety net).
- **Real Raspberry Pi deployment** (original project): native systemd install, real CH340 USB-RS485 adapter + RTU temp/humidity sensor delivering `GOOD` readings, real MQTT broker round-trip (including a real network-partition chaos test), Settings-save-restart verified under `systemctl`.
- **RTU polling-interval tuning** — the original deployment's sensor held `GOOD` down to 250ms (20x a 5000ms default) but timed out completely below that — polling limits are hardware-dependent, step down gradually on new hardware.
- **RTC hardware confirmed working on real silicon** (original deployment) — `GET /api/time` confirmed the full chain works end-to-end (`internal/time/rtc_linux.go`'s ioctls reading a real chip), not just the OS level.
- **Split Device into Connection + Device** — fixes a real shared-RTU-port corruption bug: the old flat `device` schema meant each device opened its own independent serial handle, so two devices sharing a bus corrupted each other's Modbus frames on the wire. New `connection` table holds the physical link; `device` shrank to `{name, connection_id, slave_id, polling_interval_ms, enabled}`. `modbus.Client` gained `SetUnitID(id byte)`; acquisition restructured to one goroutine per **connection**. New `/api/connections` REST resource mirrors `/api/devices`; config export/import gained a top-level `connections` array (restored before devices, matched/repointed by id — breaking change to the exported JSON shape versus the original schema). Migration auto-migrates each existing device to its own 1:1 connection (does **not** auto-consolidate — an explicit human step). Also fixed a real latent bug: SQLite's `DROP TABLE` on an FK parent with `ON DELETE CASCADE` children silently cascades under `foreign_keys=ON` even mid-migration-transaction (`PRAGMA foreign_keys=OFF` inside a `.sql` file is a documented no-op inside a transaction) — `internal/storage/storage.go`'s `migrate()` now explicitly toggles the pragma OFF/ON around each migration file and verifies `PRAGMA foreign_key_check` after.
- **Redesigned Modbus polling: continuous round-robin scan with block reads** — replaced the per-device/per-datapoint `polling_interval_ms` two-level gating scheme (a documented footgun — changing only one had no visible effect on read cadence) with a single connection-level `next_device_delay_ms` and a continuous round-robin loop. `internal/acquisition/blockplan.go` groups same-function-code datapoints by contiguous/near-adjacent register ranges (capped at the 125-register PDU limit, never splitting a datapoint's own span, tolerating small gaps up to 8 registers); FC1/FC2 (coils/discrete inputs) intentionally left ungrouped — see Todo. Migration adds `connection.next_device_delay_ms` (default 250ms, matching the known-safe floor on the original deployment's real hardware) and drops `polling_interval_ms` from both `device` and `datapoint`.
- **Devices API reports block-read count per device** — how many physical Modbus requests it takes to cover every tag each poll cycle, alongside the existing datapoint count and poll duration.
- **NTP Server field validated** (`internal/api/settings.go`'s `normalizeNTPServer`) — strips a leading `server `/`pool ` token (case-insensitive) and trims whitespace, then rejects anything still containing embedded whitespace with a `400` instead of silently saving an unresolvable value. Found because a value pasted in `/etc/ntp.conf` syntax (`server <host>`) saved successfully with no error, then failed DNS resolution at sync time and silently fell back to RTC quality.
- **MQTT ack-topic re-subscribe retry** (`internal/forwarder/mqttadapter.go`'s `onConnect`) — 3 attempts, linear backoff, then wait for next reconnect. Closes a real gap where a lost race on reconnect could leave the ack subscription missing with no retry, silently stalling Store & Forward.
- **MQTT reconnect watchdog** (`MQTTAdapter.RunReconnectWatchdog`) — forces a manual reconnect once the client has been stuck disconnected past a configurable threshold (default 45s, several multiples of paho's own 5s retry interval). Fixed a real incident where paho's own `AutoReconnect` silently stalled after a `"pingresp not received"` disconnect for 7+ minutes with the broker fully reachable. A later bug in the watchdog itself (unconditionally tearing down paho's own legitimate in-flight reconnect attempt) was found and fixed too — see HANDOFF.md bug #6.
- **`SetMaxOpenConns` raised from 1 to 4** — the real cause of a poll-duration bottleneck previously misdiagnosed as SD-card/backlog I/O contention: forcing every DB operation through one serialized connection blocked the acquisition goroutine's inserts behind slow API reads, even with WAL mode already active. A small `cycleYieldDelay` (20ms, once per full round-robin pass) was kept as defense-in-depth on top of the connection-pool fix. Raising the pool exposed a second, more serious regression — `SQLITE_BUSY_SNAPSHOT` on the forwarder's SELECT-then-UPDATE pattern — fixed with `_txlock=immediate` on the DSN. See HANDOFF.md bugs #7-8 for the full incident.
- **Web UI/API: thousands-separator number formatting, device block-read count display, various Devices/Store & Forward/Dashboard UI additions** — these UI changes lived in the `web/` frontend, not part of this repo; the backend fields/endpoints they consumed (block-read count, retention_days, database size, etc.) are.
- **Freed `/dev/serial0` for a new device** (original deployment, Pi-specific OS config, not gateway code) — the Pi's primary UART was held by the serial login console. Disabled via `raspi-config nonint`. Documented here since it's a real prerequisite step for anyone wiring a new RTU device to a serial-console-enabled Linux board.

## Todo / Out of scope

- **FC1/FC2 (coil/discrete input) decoding is broken independent of anything recent** — `DataType.RegisterCount()`'s byte-width math doesn't match Modbus's bit-packed coil/discrete-input response format. Not currently blocking anything unless a coil/discrete-input datapoint is configured, but will bite the first time one is.
- **`datapoint.word_order` is a dead field** — persisted since the first migration, never used by decode logic, never exposed in any UI. `byte_order` alone already fully specifies both byte and word order for 32/64-bit types via its permutation string (ABCD/BADC/CDAB/DCBA, plus Natural/Reversed for 64-bit). Two ways to close this, not decided: wire it up as a real independent second axis, or remove the dead column entirely.
- **`modbus.ReadWithRetry` is called with `maxRetries=0` hardcoded** even though `connection.Connection.Retry` exists, is validated, and is otherwise unused.
- **`gateway.db` doesn't shrink on disk after a retention sweep deletes rows** — SQLite frees pages internally for reuse, doesn't return them to the OS without `VACUUM` or `auto_vacuum`. Needs an exclusive lock on the whole file for its duration — time it against real traffic before running on a live device.
- **`netconfig.ApplyStatic`/confirm-revert flow**: `Current()` is verified live; the actual apply-and-auto-revert-on-timeout path is deliberately deferred until physical console access is arranged (a bad apply could lock out the only remote path to the device).
- **RTU failure paths**: unplug-mid-poll (`DEVICE_OFFLINE`) and forced-bad-response (`CRC_ERROR`) haven't been triggered from live hardware yet on the original deployment.
- **RTC DKMS auto-rebuild is inferred, not proven against a real kernel upgrade** — if this device uses a similar out-of-tree RTC driver, verify `/dev/rtc0` and `dmesg | grep -i rtc` after any kernel upgrade rather than assuming it survives.
- **Out of scope for now**: V2/V3 roadmap items (HTTPS adapter, user management, alarm management, fleet management, OTA updates). Nothing currently blocks starting any of them; just not prioritized yet.

## Current state

**Real MQTT broker now configured and working (2026-09-03/04)** — `mqtt.broker_url: tcp://mqtt.nxge.co:1883`, `transport: mqtt`, `gateway.id: GW002`. Real RTU/TCP devices are now wired up and reading `GOOD` quality (`PowerMeter`, `MD-XY02` temp/humidity sensor) — the placeholder "no connections configured" state from earlier in this deploy is over. `GET /api/store-forward/status` confirms `server_connected: true` with recent `server_last_sent_at`. Also now reachable via a Tailscale address (`100.89.94.51`) in addition to the original LAN IP (`192.168.16.253`) — see HANDOFF.md's guidance on trying a stable VPN/mesh address if a LAN IP stops responding.

**Live MQTT test-connect added to Settings save (2026-09-04)** — after the incident below, revisited the earlier decision to skip a pre-save connectivity check (see the Transport dropdown entry below and MEMORY.md's matching entry) once its real cost was seen firsthand. `saveSettings` now attempts a real, short-lived `Connect()` to the submitted broker (distinct client ID — `<client_id>-settings-test-<nanotime>` — so it can never collide with and kick the live production connection, since brokers commonly enforce "last connection wins" per client ID) whenever Store & Forward is enabled and `mqtt` is selected; a failed connect returns `400` and neither the in-memory config nor `config.yaml` is touched, so a bad save can no longer reach the next restart at all. `internal/config.BuildMQTTTLSConfig` was extracted from `cmd/gateway/adapter.go` (previously private to it) so both the real startup path and this test path share one TLS-building implementation instead of two to keep in sync. Covered by `TestTestMQTTConnect` in `internal/api/settings_test.go`, which spins up a real embedded MQTT broker (`mochi-mqtt`, matching `internal/forwarder`'s own established testing convention) for the success case, plus unreachable-broker and malformed-URL failure cases. Verified live against the real device: an unreachable broker was rejected with `400` and zero effect on the running config/live MQTT session (confirmed data kept flowing throughout); a valid save still restarts and reconnects cleanly as before. **Residual gap, called out in the UI**: this only checks reachability *at save time* — a broker that goes down between the test and the next actual restart can still crash-loop the gateway; this narrows the incident's blast radius, it doesn't eliminate the underlying fragility of "failed MQTT connect is fatal at startup."

**Incident: ~5 hour production outage from a leftover space in `mqtt.broker_url` (2026-09-03/04), now fixed** — someone edited the Broker URL field via the Settings page by typing the real broker after the original single-space placeholder instead of clearing the field first, producing `' tcp://mqtt.nxge.co:1883'`. That leading space broke `net/url.Parse`, which paho's `AddBroker` silently swallows (logged only to its own internal, silent-by-default logger) — leaving zero servers configured, so `Connect()` failed with "no servers defined to connect to", fatal at startup, crash-looping the whole gateway ~8700 times before being caught live via a `systemctl status` screenshot. Fixed the live config by hand first (stripped the space, restarted, confirmed `NRestarts` stopped climbing and real data started flowing), then shipped the actual fix: `internal/api/settings.go`'s `normalizeBrokerURL` now trims and validates `broker_url` at save time (mirroring the existing `normalizeNTPServer` pattern), applied whenever Store & Forward is enabled regardless of transport, with a dedicated regression test. Full incident writeup in MEMORY.md.

**Moved off the root filesystem onto `/userdata` (2026-09-03)** — root (`/dev/mmcblk0p7`, 5GB) was already at 77% used (pre-existing, unrelated to this gateway install — this device also runs Docker with a couple of unrelated images) and climbing, which was tripping the `storage_level: WARNING` threshold (`internal/queue/storagepolicy.go`'s `ClassifyStorageLevel`, checked against `cfg.Queue.StorageFullPercent`) even though the actual gateway install is tiny (~19MB). The device has a separate `/userdata` partition (`/dev/mmcblk0p10`, ext4, listed in `/etc/fstab` — a permanent part of the device image, not something this deploy created) with 1.5GB free and only 1% used — clearly the vendor-intended place for exactly this kind of app data. Moved the whole install there (`mv /opt/nxiiot-gateway /userdata/nxiiot-gateway`) and left a symlink at the old path (`ln -s /userdata/nxiiot-gateway /opt/nxiiot-gateway`) so `nxiiot-gateway.service`'s `ExecStart`/`WorkingDirectory` and every doc/deploy-script reference to `/opt/nxiiot-gateway` keep working unchanged — no systemd unit or script edits needed. `storage.DiskUsagePercent` resolves through the symlink automatically (confirmed via `GET /api/system`: `disk_used_percent` dropped from 76.6% to 1.25%, `disk_total_gb` now correctly reports `/userdata`'s 1.58GB instead of root's ~5GB). **Any future fresh deploy to this device (e.g. after a reflash) should install straight to `/userdata/nxiiot-gateway` with the same symlink from the start**, rather than installing to `/opt` and moving it after the fact.

**`web/` (React 19 + Vite + TS frontend) brought into this repo (2026-09-03)**, split from the original monorepo (`https://github.com/bpp-pico/nxIIoT-Gateway.git`) via `git subtree split --prefix=web` + `git subtree add`, same approach as the original `gateway/` split — full frontend history preserved. Brought in specifically because the Devices/Config pages' enable/disable controls for connections/devices (`web/src/components/ConnectionForm.tsx`, `DeviceForm.tsx`) needed a way to actually reach this device; the REST API alone has no UI.

**Static frontend serving added to the Go API** (`internal/api/router.go`, `internal/config/config.go`) — a new opt-in `api.static_dir` config field. When set, the router mounts `http.FileServer` on `/*` (after the `/api` route group, so it never shadows an API route) to serve the `vite build` output on the *same* `:8080` port as the REST API. Chosen over the monorepo's own dev-only approach (`web/deploy/nxiiot-gateway-web.service` runs `npm run dev` — a persistent Vite **dev server** — in "production" on the original Pi) because: **this device has no internet access at all** (no default route — ruled out `apt install nginx`), no Node/npm installed (and is meant to stay dev-toolchain-free per explicit instruction), and the frontend has no client-side routing (`web/src/App.tsx` is a single tab-state SPA, no react-router) so a plain FileServer needs no SPA-fallback logic. No new process, no reverse proxy, no CORS — same origin by construction. Empty (default) `static_dir` fully preserves old behavior for any other deployment of this binary.

Frontend built locally (`cd web && npm install && npm run build`, ~260KB total) and deployed as static files to `/opt/nxiiot-gateway/web-dist` on the device, same cross-compile-and-SFTP pattern as the Go binary. Verified: `GET /` → 200, `GET /assets/*` → 200, and the enable/disable control's actual backend behavior end-to-end via the API directly (created a test TCP connection+device with `enabled:true` → `acquisition reloaded active_connections=1` in the logs and a real (refused, expected — nothing was listening) connection attempt; set `enabled:false` → reloaded back to `active_connections=0`; test data deleted after). **Not yet verified visually in an actual browser** — no browser tooling was available to confirm the UI renders/behaves correctly, only that its underlying API calls work.

**Deployed to the real MaxGate400 device (2026-09-03)** — `root@192.168.16.253` (hostname `welin`), Ubuntu 20.04.2 LTS, armv7l/armhf, 474MB RAM, kernel 6.1.99. Running as `nxiiot-gateway.service` (systemd, `Restart=always`) under `/opt/nxiiot-gateway`, `gateway_id=MaxGate400`. Confirmed stable via `systemctl status`/`journalctl` (`NRestarts=0` after settling) and `GET /api/system` responding `{"status":"ok",...}`.

Deploy deviated from README.md's documented native-build steps because **the device has no Go toolchain installed** (`go: command not found`) and is intentionally being kept dev-toolchain-free (production-only device, per explicit instruction — any dev work happens elsewhere). Built instead via cross-compile from the Windows dev machine: `GOOS=linux GOARCH=arm GOARM=7 CGO_ENABLED=0 go build` — safe because every dependency in go.mod is pure Go (`modernc.org/sqlite` needs no cgo), so the binary is fully static (confirmed `file` output: statically linked ELF). Binary + `configs/config.yaml` + `migrations/*.sql` + `deploy/nxiiot-gateway.service` were pushed via SFTP, not `git pull`+build-on-device as README describes — the target device's own git checkout was not used for this deploy.

**Deliberately deferred, not yet configured on this device:**
- **No connections/devices configured** — deploy went out empty (`active_connections=0`); to be configured later via the REST API/Web UI once real Modbus wiring is available (no RTU device was physically attached during this deploy — no `ttyUSB`/`ttyAMA` visible).
- **The Settings page has a "Store & Forward" master switch (Enabled checkbox)** (2026-09-03) — the requested "just read Modbus" mode. Backend: `config.StoreForwardConfig.Disabled` (zero-value `false` = enabled, so existing config.yaml files with no `store_forward` section keep today's behavior). When disabled, `cmd/gateway/main.go` skips `proc.Process` entirely in the acquisition callback (so nothing is written to `data_queue`) *and* skips constructing the adapter/Forwarder altogether — no MQTT/HTTP connection is attempted at all (`fwd` stays `nil`, which `internal/api/storeforward.go` already handled gracefully before this change). Acquisition itself is untouched: Modbus polling, `statusStore` live values, and the `"reading"` log lines keep working exactly as before, satisfying "just read Modbus" without needing a separate always-on read-only mode elsewhere. Verified live: created a test TCP device while disabled — repeated poll attempts logged every ~1s, `GET /api/store-forward/status` stayed at `pending_records: 0` throughout; re-enabled afterward, confirmed normal state restored, test connection/device deleted.
- **The Settings page's MQTT Server card now has a Transport dropdown** (`http`/`mqtt`) — `forwarder.transport` was previously a config.yaml-only field with no UI surface at all. Backend: `settingsDTO.MQTT.Transport` in `internal/api/settings.go`, validated to exactly `"http"`/`"mqtt"` (rejects anything else with `400`, no restart triggered on a rejected save). Deliberately **no pre-save "test broker connection" check** (unlike `netconfig`'s confirm-or-auto-revert pattern) — a plain dropdown was chosen instead, with a warning shown in the UI when `mqtt` is selected: an unreachable/wrong Broker URL saved with `transport: mqtt` will fail at gateway startup before the API (and thus this same web UI) comes back up, per `cmd/gateway/adapter.go`'s fatal-on-connect-failure design (see above/MEMORY.md). If that happens, recovery needs SSH access to flip `configs/config.yaml` back to `http` and restart manually — there is no auto-revert safety net here.
- **MQTT broker not yet known.** `forwarder.transport` is temporarily set to `"http"` (with an empty, unused `server_url`) instead of the intended production `"mqtt"` — **not a config oversight, forced by a real startup-ordering constraint**: `cmd/gateway/adapter.go`'s `buildAdapter()` deliberately makes a failed MQTT `Connect()` **fatal at process startup** ("a gateway silently not forwarding is worse than one that fails loudly" — see its doc comment), which crash-loops the *entire* process (acquisition + API included, not just forwarding) when no broker is reachable. The `http` transport path never connects at startup, so it was used as a safe placeholder that keeps the gateway (Modbus acquisition, RTC, the API) up and running while MQTT is still unconfigured. **Action needed**: once the real downstream MQTT broker URL/credentials are known, set `mqtt.broker_url`/`username`/`password` in `/opt/nxiiot-gateway/configs/config.yaml`, set `forwarder.transport: "mqtt"`, and `systemctl restart nxiiot-gateway` — verify it stays up (not crash-looping) afterward given the fatal-on-connect-failure behavior above.
- `time.timezone` left at the dev default `"UTC"` — not changed, wasn't part of this deploy's scope; revisit if local-time display/logging matters for this site.
- `log.level` set to `"info"` for this deploy (dev config had `"debug"`).

Whoever picks this up next should: get the real MQTT broker URL/credentials and flip the transport back to `mqtt` (see above), then work through the RTU/RTC/network-IP hardware verification steps fresh against this MaxGate400 device once a real RTU device is wired up — don't assume the original Pi deployment's results (e.g. the 250ms polling floor) carry over as-is; step down gradually per this repo's own MEMORY.md guidance.

## Data Contracts

Interfaces this gateway depends on — flag before changing either of these, since any downstream MQTT consumer depends on them matching exactly.

- **MQTT data topic** (gateway → broker): `gateway/{gateway_id}/data`, QoS 1, JSON:
  `{ "batch_id": string, "entries": [{ "gateway_id": string, "sequence_id": int64, "device_id": int64, "datapoint_id": int64, "value": float64|null, "quality": "GOOD"|"CRC_ERROR"|"DEVICE_OFFLINE"|"INVALID"|"TIMEOUT", "event_timestamp": RFC3339, "priority": string }] }`
  (`internal/forwarder/wire.go`'s `WireEntry`.)
- **MQTT ack topic** (consumer → gateway): `gateway/{gateway_id}/ack`, QoS 1, JSON: `{ "batch_id": string, "error": string (optional) }`. Any consumer must derive this from the data topic it received (`strings.TrimSuffix(topic, "/data") + "/ack"`), not hardcode a gateway ID.
- **Gateway REST API**: connections (`GET/POST/PUT/DELETE /api/connections`), devices (`GET/POST/PUT/DELETE /api/devices`), and datapoints (`GET/POST/PUT/DELETE /api/datapoints`). A device carries only `connection_id`/`slave_id`/`polling_interval_ms`/`enabled` — protocol/interface/baud/etc live on the connection it references. `DELETE /api/connections/{id}` returns `409` while any device still references it. Config export/import (`GET/POST /api/config/export|import`) carries a top-level `connections` array restored before `devices`.
